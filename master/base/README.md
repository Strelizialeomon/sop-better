# {{project_name}}

项目开发约定入口：

- `AGENTS.md`：每次进入项目都会加载的核心约束。
- `.sop/profile.json`：项目端、人、风险和默认分支的事实。
- `docs/project/issue-pr-workflow.md`：doc / issue / PR 的执行流程。
- `docs/decisions/`：长期或跨边界决策。

Git 工作基线是 `origin/{{default_branch}}`；实际操作前仍先看当前分支和本地未提交改动。
