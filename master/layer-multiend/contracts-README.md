# 跨端契约（单一真相源）

端之间共享的字段、接口、事件和错误码，只在本目录定义一次。端清单以 `.sop/profile.json` 为准。

## 规矩

- 改跨端事实时，先改契约草案，再通知相关端并完成握手。
- 契约 freeze 需要 owner 明确确认。
- 只服务一次 feature 的草案可留在 issue；会被未来功能复用的平台边界才沉淀成本目录文件。

## 契约文件格式

每个主题一份文件，至少包含：

- 涉及端与版本。
- 字段、接口或事件，以及谁写、谁读。
- 错误与兼容边界。
- 变更记录和验证证据。
