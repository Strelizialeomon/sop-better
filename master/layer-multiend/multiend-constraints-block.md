<!-- master/layer-multiend/multiend-constraints-block.md —— 触发:有第 2 个端(≥2 端)。
     /sop-init 把这几条「追加」进项目根 CLAUDE.md 的「§2 Agent 工作约束」块末尾(在 base 标准块基础上加)。占位符:{{ends}}
     拆分自原 templates/agent-constraints.md「多端追加块」(标准块 + 沟通约束块已嵌进 master/base/CLAUDE.md)。-->

<!-- 追加到项目根 CLAUDE.md 的「§2 Agent 工作约束」末尾(仅多端): -->
- 端划分：{{ends}}。每端有自己的 scope，跨端走 `docs/contracts/` 的单一真相源。
- 各端独立 brainstorming；跨端契约 freeze 需我明确确认。
- 状态用标记：✅ 完成 / 🚧 进行 / ⏸️ 搁置 / ⬜ 未开始。
