# sop-better STANDARD —— 开发 SOP 的权威尺

`$sop-init` 据此**生成**,`$sop-audit` 据此**对照**。本文件是唯一真相源。

> 状态:已验、持续迭代——exp-002 起在真实项目(geo-reverse / taoxi-geo / media-ops)跑通(证据见 `PLAYBOOK.md`)。决策 **(A) 两层模型(公约+实例化)** 与 **(B) "客观反驳"列为撒手安全条件** 均已确认并承重(§0 / §1.4)。

> 术语:下文 **agent 指令文件** 固定指 Codex 会自动读取的 `AGENTS.md`。本工具不再生成或维护 `CLAUDE.md`。

---

## 0. 两层模型(本工具的地基)

一份 SOP = **不变的公约** + **按项目参数化的实例化**。`$sop-init` 两样都产出;既不死模板,也不空泛宪章。

```
$sop-init = 固定公约(照搬) + 读本项目的 [端/协作人/风险] 参数 → 生成具体角色与结构
```

---

## 1. 公约(不变层 · 任何档照搬)

> 🧭 **元规则(统领一切 · 地板不是天花板)**:下列公约是**安全地板,不是思考天花板**。照字面会让结果变差、或情况没覆盖到 → **用判断按精神来 + 标记一句(不静默跳过)**,owner 终裁。规则为协作 + 撒手 + 右尺寸服务,**别为守规则而守规则、别当规则机器人**。(承重墙——反驳 / 凭据保真 / 不擅自 push / 高风险闸——仍是硬地板,不在此豁免。)

> **三分法(统领下列公约 · owner 的 mental model)**:SOP 的内容分三类——
> - **定义**:硬约束 · agent **自动执行不容差错**(issue/PR/review 流程、分支、单一真相源……)。人不用管。
> - **建议**:agent **主动补人的盲区**(技术可行性 / 业务最佳实践 / 怎么细化拆分)· **照亮不代选**。帮人想,不替人定。
> - **决策**:人定"要什么" · **不可压缩的人核**。
>
> 按人掏成本:**定义=零 · 建议=低 · 决策=不可压缩**。**终极目标:把"决策"压到最小,定义全自动,建议帮人想全——人只剩 informed 决策这一件最该做的事。**

跟项目无关,solo 脚本和四端产品都一样:

1. **人机分工**:人 = 要什么(目标/约束/验收)+ 否决权;AI = 怎么做(架构/设计/拆解/实现)。人是审稿人,不是作者;但看得见、能毙、高风险能跳进去(**不从 L0 翻成盲盒**)。
2. **重瞄 brainstorming(随角色变焦)**:agent 跟人确认什么,**看人扮什么角色**。**起手先定角色**:agent 指令文件没钉死单一角色就问一句「你这次是业务还是开发?」(默认业务 · 见下 owner 陷阱),别靠猜。变焦如下——
   - **业务 / 需求角色**(定产品要什么)→ 只梳"**要什么**"(目标/约束/验收),**别把他拖进技术架构**;架构 AI 提案、他评审。这是默认,也是本工具初心(病根=brainstorming 把人脑内的代码抽出来)。**产品形态(网页 / 后台按钮 / App 页面 / 定时自动跑…)属"要什么"、该业务定,必须用业务话确认"长什么样"——别让它默认成开发顺手的形态(如命令行 · 反例 geo-reverse #2:不懂技术的需求方"确认"出了 CLI,这是错的)。**
   - **开发角色**(人本身是技术决策者)→ agent 应**和他确认技术方案 / 架构**(用 brainstorming 时尤其);此时人确实该参与 how。
   - ⚠️ **owner 陷阱**:你若习惯性戴"开发帽",agent 就会一直跟你确认架构 → 你又回到想逃的过度投入。要拿杠杆,**默认把自己摆业务角色**,想技术介入再主动戴开发帽——别让习惯替你戴。
3. **跳 writing-plans + 补偿**:不跑 writing-plans;代价用两条硬补——**spec 验收标准必须可检验** + **code review 必留**(撒手后的安全网)。「plan 越轻 → 验收越硬 + review 越严」。**这条管所有生成物**:连协作/流程 SOP 里也不许把开发流程建在 writing-plans 上(exp-002 翻车点)。**review 用「新眼睛」**(吸自 taoxi-geo):别让写代码的 agent 在自己有偏见的上下文里自审——派**独立子代理**当白纸,喂 spec/验收 + diff + 决策快照,**三查**(合规 / bug / 优化),非阻断直接改、阻断才 escalate。**新眼睛不只审代码、也审 spec**(exp-016):spec 收口后、交实现前,过一只同样的独立子代理审 **spec 本身**,三查——① **实证 + 信源可信 vs 拍脑袋**(关键事实 / 数字 / 现状假设:实测过 / 引**权威一手源**〔官方文档·规范·源码〕,还是直觉拍 / 把 **GH issue·SO 当事实**?拍错代价大的点出来 + 指明该去测 / 核实什么)② **验收可机检**(不可证伪口号 → 改可观测断言)③ **范围 / 形态 / 边界**(权限 / 隐私 / 规模 / 错误处理有没有埋窟窿);**findings 是「待核实的假设」**、交作者 / owner 解,非阻断、严重才 escalate。**琐碎 spec(改一行 / 纯查事实 / 无真环境可摸)免**(反过度治理)。把 sop-better 自用的「真跑>dry-run」(exp-002/013/014)下发成项目护栏——最载重的 spec 此前只过 owner 收口、缺独立眼。**需求/spec 收口标准**:对话到「**验收 + 主流程 + 范围(要啥/不要啥)+ 形态(交付长什么样、业务话定)四样钉死**」即收口(不是聊到完美);收口后球交 agent。
4. **🧱 客观反驳(承重墙 · 与 code-review 并列)**:撒手给 yes-man = 把判断力交给讨好者,比不撒手更糟,所以"敢反驳"是**让撒手成立的安全条件**。但"要客观"是口号、不可检查、没用——必须落成**强制输出 + 闸门**的协议:

   **反驳协议(6 闸,可检查):**
   - **A 空夸即违规**:出现"完美/对/戳中/great"而不跟一条**可证伪理由** = 违规,要么补理由要么删。
   - **B 批准前先出反方**:评审任何方案/决策,**先给最强反对 + ≥1 具体风险,再给结论**;"没问题"不能当第一反应,真没异议要说明"查了什么才没找到"。
   - **C 同意标来源**:区分"我独立判断也认同(因为…)"和"你倾向它、我只是没强烈反对",后者必须明说,不许伪装成前者。
   - **D 守住一轮再让**:判断用户错且有后果时,第一次被反驳**不许秒折**;核心理由再讲一遍,要用户**明确 override**;override 后照办但把异议记进 ADR/实验记录。最终否决权仍归用户。
   - **E 标信心 + 标未验**:每个非平凡判断带"信心几成 / 什么会让我改主意 / 没验的部分"。
   - **F 高危加倍**:不可逆 / 高 stakes / 用户正上头时,B、D 必须主动触发。

   钥匙:把异议做成**必须产出 + 过不去的闸**,不是态度——态度管不住(本项目作者亲测:写完"禁止讨好"下一条仍在空夸)。`$sop-audit` 把"agent 被设成顺从无异议"列为**不合理**。
5. **右尺寸 = 价值 vs 人的成本(不是结构多少)**:SOP 该多重,看它**让人掏多少**(等 / 判断 / 手填),**不看结构量**。**人在跑的仪式**(你手开 issue、贴 label、等每个 PR)→ 吃你杠杆 → 过度治理;**AI 自动跑的仪式**(agent 替你开/标/流转/提 PR)→ 人几乎不掏钱却白拿可追踪可恢复 → 划算,不是过度。**自动化把成本从人挪到 AI,同样的结构就从"过度"变"划算"。**
6. **单一真相源 + 决策留痕**:每条跨端/长期决策进 `docs/decisions/`(ADR);事实只在一处权威定义,别处引用不重声。
7. **说人话(通俗易懂)**:agent 对人输出要**结论先行、少黑话、多打比方、短句分点**;术语/英文缩写首次出现先一句话解释;不确定用户懂不懂就往简单了说。与反驳同属**沟通约束**——反驳管"敢不敢说真话",说人话管"说得人听不听得懂",两者都管。**且与反驳同理(§1.4)——口号管不住、必须落成可检查的闸(黑话 / 结论先行 / 大段),权威闸在 `master/base/AGENTS.md` 母本沟通约束块(生成时落入 `AGENTS.md`)、`$sop-audit` 据此查。** **措辞分受众(都中性专业、不俏皮)**:给人看的(对话 / 报告)= 通俗但职场,不插科打诨;给 agent 看的(spec / issue / 约束 / 凭据)= 精确无歧义中性——俏皮口语(如"顶嘴")对 agent 是噪音、还可能歧义。领域术语 / 比喻可留(帮理解),纯逗乐的俏皮不留。

8. **🗂 凭据 = 撒手的基础设施 + 必须保真**:issue/PR/doc/ADR 是 **agent 之间的共享内存**——agent 开需求、留言跟进、tag、熟练操作它;**人可读可不读**。agent 凭据越完善,你能撒手越高(它从**凭据**重建上下文,不从**你**)。**但凭据的命门是"真"**:会漂移 / 说谎的 issue 比没有更危险(agent 会笃定照错的凭据行动)。故必须——状态真反映进展、不许烂尾成"开着没人管"的坟场、关键节点一道核对(人或审计 agent)确认"写的 = 实际发生的"。为 **agent 解析+操作** 而设计,但人想看随时看得懂。**协作三件套**:doc=正文 / 真相源(需求、设计、契约、长期决策);issue=索引 + 状态 + 消息总线(谁在做、等谁、变更去哪看);PR=交付凭据 + 验收 / 收口动作(改了什么、怎么验、是否 `Closes`)。口诀仍是 **issue 薄、doc 厚**。标准工作流见 `master/base/docs/project/issue-pr-workflow.md`。**保真是双向的**:生产者**先把 doc commit+push 到远端、再开 issue 指过去**(指稳定引用 commit permalink / 已合 PR,别指会悬空的分支相对路径);消费者**起手先验链接解析得开**(fetch 远端、按你干活的 ref 找 doc)——**悬空链接 = 会说谎的凭据**(issue 写"详见此 doc"、doc 却不在),按 §1.9 反弹回生产者,**不自己补**。**放哪(house-style · 防各项目飘)**:carve-out / 凭据原则进 agent 指令文件(always-loaded、开工必在眼前);操作闸进 workflow doc,agent 指令文件留一句指针;**消费验链接挂进「起手 freshness」那一步**(开工本就 `git fetch` + 看 issue,顺手验它引用的 doc 链接打不打得开),别单列成易漏的孤立规则。(geo-reverse 把两闸都塞 agent 指令文件、media-ops 把生产闸留 workflow doc,都跑得通;定此口径只为未来项目别飘。)

   **凭据的读写有两个人-会话书挡(所有项目都走 issue+PR、故都适用 · 不是新概念,就是本条「开工读 / 收工写」的两端)**:**开工**——标 `待业务确认` 的凭据(等人拍板的 issue)要**主动 surface 给人**、别让决策烂在 issue 里没人推(现模型 agent 只会按 scope 拉活,缺这一推);**收工**——owner 说"收工 / 结束"即上面红线「不擅自 push」要的那个**明确指令**时点,把本会话收口的 doc 按 workflow 推远端。**文档(纯"要什么"的 req/design doc)是低风险凭据,内容审在 brainstorming 收口、不在 PR 再审一遍 → 默认直接推 main(main 受保护则开 PR 但 agent 自审自动合、人不等);代码不吃这条豁免,照旧 branch+PR+新眼睛 review。**
9. **🤖 agent 自决 / 永不阻塞**(吸自 taoxi-geo · "agent 自决=不费事":成本在 AI 不在人,能自决就自决,只在真该停处停):
   - **不假民主**:SOP / agent 指令文件 / 已定方案 / owner 指令任一已覆盖的事 → **直接执行,不 surface 给人 pick**。列一堆方案 + "我倾向 X" + 让 owner 拍 = **"假民主真 ask" 反模式**(破坏节奏 + 架空 SOP)。SOP 没定才 escalate。
   - **🔀 escalate 是自决动作、不是选择题(不假民主 carve-out)**:识别出要改 req doc / 扩冻结范围 / 跨端 → **"把它上交对的人(多端多人有 coord 就给 coord、双角色就给业务方)"这个动作(写 issue 评论 + 附设计草案)是 agent 自己该做的**,不请示。拆两半:**做不做 / 优先级 = 产品决策、归 owner(§1.1);怎么送到对的人手里 = 机械动作、agent 自决**——自己写完报一句("我给 coord 写了条评论"),别把"要不要 / 怎么 escalate"也做成 owner 的 pick(= 假民主真 ask + 逼 owner 手把手驱动、吃掉撒手杠杆)。**两条边界**:① 草案只到 how(技术怎么改),**别替业务把 req doc / 需求替写了**(那是角色边界 carve-out 禁的伪造真相源);② **上交 ≠ 获批施工**——递出去后"做不做 / 并进哪期"等 owner+coord 回,别自己接着开干。**owner 当面交办的扩范围 / 跨端需求同样走这条**:别因"老板亲口说"就吞进自己的活、悄悄并进自己 PR。
   - **自决口诀**:`局部补丁 → 直接改;要涟漪出去 / 要重做 / 跨边界 → 找人`。
   - **🏗 立栈不算边界细节(自决 carve-out)**:新端/新服务**起步**的技术基建选型(语言/框架/项目布局/核心基建依赖如 ORM/配置/日志)= 口诀里的"要重做"级涟漪,不归自决——greenfield"还没东西可涟漪"是错觉,立错了就是整端重铺(反例 media-ops #60→#62:agent 自决裸写 stdlib,owner 反向 ~900 行重铺)。有 `house_style`(§2)参照 → 直接对齐(定义层,零人成本);没有 → 出**可扫提案**(每决策 + 一句理由,2 分钟能审完)**阻塞等 owner 拍板**再动工。**边界**:既有栈内加常规依赖/库 = 照旧自决,不许把此闸扩成"每个依赖都来问"。
   - **永不阻塞**:实施中遇 spec 没 cover 的**边界细节** → 自决最合理实现 + 评论留痕,不退回、不卡 owner。**只在这几种仍停下问**:违反已冻结契约 / 严重偏离目标(推翻路径而非微调)/ 改跨端骨架 / 越界动他人 scope。
   - **🚧 永不阻塞 ≠ 替别人补缺的交付物(角色边界 carve-out)**:上一条"自决"**只管边界细节**;若缺的是上游**别的角色该给、却缺失 / 链接悬空**的交付物(需求 doc / 契约 / 验收)——**这不是可自决的边界,是坏掉的交接**。别自己造(自造 = 伪造真相源:照一份没人认的"需求"笃定行动 = **隐蔽地做错,比卡住更糟**),**反弹回该角色**(issue 评论 tag + 标 ⏸️ 待澄清),手上其它没被卡的继续做。"永不阻塞"管的是「别为你权限内的决策等人」,**不是「别人没交的东西你替他造」**。(geo-reverse issue #2 真坑 · exp-004:缺 req doc,开发 agent 凭"永不阻塞 + 补盲区"自己把需求写了。)
10. **💡 主动建议 / 补盲区(照亮不代选 · 三分法的"建议"层)**:人(尤其非全栈 / 不熟业务)常连"要什么"都拿不准——不懂技术可行性、不懂业务最佳实践、不知怎么把项目细化拆分。agent 应**主动出建议补这些盲区**,让人的决策有依据(这补上了"agent 不光接管 how、还帮人想 what")。**纪律:列选项 + 讲权衡 + 点盲区(撑大人的视野),但不替人拍板**——"我倾向 X 你点头吧" = 建议塌成"假建议真决策"(同 §1.4 反驳 + §1.9 不假民主)。照亮,不代选。

**§1 附:运行时协作闭环(查证 → 分流 → 调研 → 执行验证 → 收口)**:这是 §1.4 反驳 / §1.7 说人话 / §1.9 自决 / §1.10 主动建议的执行层。① **查证(诚实无知闸)**:关键信息缺失时先查,查不到就说不知道,影响结果就打断确认,不替用户找补。本地事实查代码 / 配置 / 日志 / issue / PR / 运行结果;外部事实涉及最新信息 / 第三方 API / 平台规则 / 依赖版本 / 价格 / 安全 / 部署 / 合规 / 外部最佳实践时联网查可靠来源;查不到 / 不能联网 / 无权限 / 没实测,标 `未联网 / 未实测 / 未查到 / 无权限`;用户前提明显可能错且有后果时按反驳协议指出并守住一轮。八荣八耻只作记忆钩子:瞎猜→查证,模糊→确认,臆想业务→用户确认 / 上游反弹,重复创造→复用现有,跳验证→测试或标没跑,破坏架构→遵循规范,假装理解→说不知道,盲目修改→先想清后果。

② **分流(三层引导)**:看 agent 下一步动作的后果,不看用户措辞。只读 / 解释 / 状态 / 调研 / 看 diff → 直接答或查,结尾给 1 个推荐下一步;真有分叉才给 2-3 个选项。下一步会改本地状态,且目标 / 范围 / 验收 / 背景缺口会影响实现,或跨边界 / 不易回滚 / 新增或替换核心基建依赖 / 跨栈依赖 / 影响安全、成本、许可、部署的依赖 / 改核心配置 / 改 SOP / 用户明确说先讨论 → 给确认卡并等 owner。用户已明确要做、范围清楚、可回滚、只动局部 → 先说明范围再直接改。push / deploy / delete / 改 git 历史 / 生产环境 / 付费 API / 大范围重构 → 高危闸等明确确认。

③ **调研(方案前调研包)**:给方案 / 设计 / 估算 / 选型 / 外部平台接入前,先产出轻量五件套:**调研结论 + 主流做法(2-4 种) + 建议方案 + 信源凭据 + 未验证 / 风险**。信源顺序:真实环境实测 / 官方文档 / 源码 / 标准规范 > release note / changelog > GitHub issue / StackOverflow / 博客;后者只当线索,关键结论回一手源或实测复核。低风险局部补丁、纯解释、稳定常识、无真实环境可摸的小事不为调研而调研;无法联网 / 无法实测时明说未验,再给保守建议。

④ **执行验证 + 收口(任务收口卡)**:实施后主动跑可用验证;不能跑就说明没跑和风险。每件事结束都要判断状态、验证、风险和下一步:小事一句话收口;普通任务交代当前状态(完成 / 基本完成 / 卡住 / 等 owner 决策)+ 验证结果 + 风险遗留 + 推荐下一步,真有分叉才给 2-3 个快捷选项;高风险 / 多分叉任务给决策收口卡。推荐不等于授权:push / deploy / 删除 / 改 git 历史 / 生产 / 付费 API 等仍按高危闸。别为小事输出长卡片,别强行造选择题,无法验证就明说未验。

---

## 2. 实例化层(可变 · 每项目参数化)

`$sop-init` 询问或接收这些参数,据此生成,**不硬写**:

| 参数 | 例 | 影响 |
|---|---|---|
| `ends[]` | admin / backend / frontend / mobile / crawler | **≥2 个端 → 触发多端结构**(契约 + 每端身份文档 + worktree);单端不建。生成对应 scope 角色与目录(见 §3) |
| `collaborators[]` | 单人 / 人+多 agent / 多人 | **≥2 个不同的人 → 触发协作 doc**(谁干啥不撞车);单人不建。**issue+PR 不看这条**——归恒定流程(见 §3) |
| `risk` | 可逆低风险 / 线上不可逆 | 默认撒手档、review 严格度;高风险闸恒定 |
| `house_style[]` | Go 后端 → go_dispatch_backend 仓**本体**(不指技能/记忆快照,快照会过时)/ 无 | 立栈直接对齐(定义层);无参照 → 触发立栈确认闸(§1.9) |

> agent 运行时不再是参数:`$sop-init` 只生成 Codex 的 `AGENTS.md`;已有 `CLAUDE.md` 视为旧残留,由 `$sop-audit` 报告迁移 / 删除。

---

## 3. 一条流程 + 结构按现实长(不预先枚举档位)

> ⚠️ 旧版用「两根档位轴 × 3 值 = 9 宫格」(端数 S0–S2 × 协作 C0–C2,外加 T0/T1/T2 简称)预先枚举组合。**枚举是多余的**:§1 公约那条流程**本就跟项目无关、solo 脚本和四端产品一样**(见 §1 开头那句),档位真正决定的**只有结构**。故收掉枚举,回到生成规则——**结构按"实际有什么"长出来,没有的绝不预建**(exp-012)。
>
> (更早的历史:曾把档位压成一根 T0/T1/T2 标量,被 geo-reverse「1 端 + 业务/开发 2 角色」照出漏洞——端数与人数是两回事,一根尺装不下〔exp-002〕。后来拆成两轴 9 宫格仍是过度枚举;新模型用两条**正交触发**根治:端的事归端、人的事归人。)

**流程恒定 · 所有项目同一条** = §1 公约的执行面:
起手 freshness → 单一真相源 → 重瞄 brainstorming(角色变焦)→ 跳 writing-plans 但 spec 验收可检验 → **issue+PR**(人人都走,含单人——agent 开/标/流转/提/低风险自动合,成本在 AI 不在人 · §1.5)→ 高风险人审 → 必过独立新眼睛 review → 高风险闸(线上不可逆,恒定)。**这条不随任何参数变。**

**结构按正交触发长出来(绝不为还没有的预建 · §3.5):**

- **有第 2 个端吗?**(前端+后端 / +小程序 / +爬虫…)→ 才加:`docs/contracts/`(跨端真相源)+ 每端身份文档 `<端>/<agent 指令文件>`(`end-role-agent.md`)。**这些端再开"真并行多 agent"(= 上 worktree)吗?** 上 → 再加 worktree 隔离(`worktree-isolation.md`)+ **多 agent 协调骨架**(6+1 / 消息总线 / scope 隔离 / coordination 角色 · `collaboration.md` 多端追加段);串行 / 单 agent → 这俩不加。单端 → 一概不要。
- **有第 2 个人吗?**(业务↔开发,各带 agent,靠 issue+doc 协同)→ 才加:`collaboration.md` 的**业务↔开发 handoff**(起需求 / 收口标准 / 交棒)。单人 → 不要(角色变焦在 §1 公约里恒定;**issue+PR 不归这条、归恒定流程**)。

**触发互不相干**:media-ops = 单端 + 2 人(有业务↔开发 handoff、无契约);**taoxi-geo = 单人 + 多端 + 真并行多 agent**(有契约 + worktree + 协调骨架,但无业务↔开发 handoff——**多 agent 协调挂"并行 agent"、不挂"第 2 个人"**)。`collaboration.md` 在"多人"或"多端并行多 agent"**任一**命中时生成、带对应的段(可只有其一)。

**口诀:契约/按端文档 看「有没有第 2 个端」,worktree + 多 agent 协调骨架 看「要不要真并行多 agent」,业务↔开发 handoff 看「有没有第 2 个人」;流程(含 issue+PR)恒定,公约(§1)全适用。**

---

## 3.5 演进:项目长大,结构逐层长

项目会移动——起步单端单人,后来加端、加人。结构跟着长,铁律:

- **开始最小**:没第 2 个端就别建 contracts/按端文档/worktree;没第 2 个人就别建协作 doc。**绝不为还没有的端/人预建结构**——为想象建仪式,是**最坏的过度治理**。
- **写下升级触发条件**:在 ADR 记两条触发何时越过——"加第 2 个端 → 补 contracts/按端身份文档/worktree;加第 2 个人 → 补协作 doc"。这是项目长大的指路牌,也是给未来 agent 的凭据。
- **真触发才补那一层**:加了前端(单端→多端),重跑 `$sop-init`(增量,只补新端的契约/身份文档、不砸已有);或 `$sop-audit` 会逮到「结构错配」(多端了却没契约)。
- **SOP 规则也按需长**(吸自 taoxi-geo「阈值上提」):一次踩坑只在评论/记录留教训,**复发 2+ 次成模式才正式改规矩**——别为一个数据点改标准,否则 SOP 自己膨胀(taoxi-geo 829 行的教训)。**反向齿轮(只会加挡 → 攒成笼子)**:规则**也按需删 / 合 / 降级**——加一条前先查能否并进已有(别新立);`$sop-audit` 或定期自审时,查"从没触发 / 被别条 subsume / 已内化"的**死规则 → 删之**。**SOP 要允许掉秤,不只长肉**(对 STANDARD 自己也递归生效;本次 exp-012 收掉档位枚举就是一次掉秤)。
- ⚠️ **诚实限制**:加端不总能干净"追加"。单端→多端若接口写死在代码里,要把隐含契约**抠出来 = 返工**。工具能搭新层 + 标出"哪些得抠",**不替你自动重构**。

---

## 4. $sop-init 生成的「Agent 工作约束」块

权威版本在 **`master/base/AGENTS.md` 母本**(生成时落为 `AGENTS.md`;工作约束块 + 沟通约束块;多端追加在 `master/layer-multiend/multiend-constraints-block.md`,按"有没有第 2 个端"选用)。此处不放示例正文——快照会过期(§1.6 单一真相源;本节旧示例就曾落后模板一代)。

---

## 5. $sop-audit 的查法(头号查"太重")

按严重度查"不合理":

1. **过度治理(头号)**:对项目规模而言仪式过重(**无并行多 agent** 的项目却背着 coordination/scope label、单人却装多人协作机器、回溯 req doc/audit 补口)。**也含"规则攒成笼子"**:约束块 / SOP 里有 agent 从不照做 / 与别条重复 / 已被 subsume 的**死规则** → 该删 / 合 / 降级(§3.5 反向齿轮)。
2. **结构错配(按触发各查)**:① 端——多端却没契约 / 没按端身份文档,或单端却凭空建了契约;② 并行——多端真并行多 agent 却没 worktree / 没协调骨架(6+1/消息总线/scope/coordination),或无并行多 agent 却背着这些(预建);③ 人——多人却没业务↔开发 handoff doc,或单人却装多人协作机器。**该有的缺位、不该有的预建,都算。**(issue+PR 是恒定流程、不在此查——见 §5.1 铁律:agent 自动维护的不算过度。)**④ 删除残留(exp-013)**:项目还留着 STANDARD / 模板**已删的块 / 档 / 文件**(已废的极简块、`S/C/T` 档位编号、`collaboration-c2` / `s2-contracts` 等已删 / 改名)= 漂移、该**换成现行块**。减法后旧产物的"删除残留",**单向只查"缺新护栏"会放过它**——审计必双向比(`$sop-audit` 模板版本差 · 减法时尤其查)。
3. **沟通闭环缺环 / 口号化 / 过度阻塞**:按 §1「查证 → 分流 → 调研 → 执行验证 → 收口」查。① 反驳 / 说人话只写态度,没落成先反方、标来源 / 信心 / 未验、黑话 / 结论先行 / 大段等可检查闸 = 口号化;② 查证缺环:只写"不要瞎猜 / 不懂就问",却没要求本地事实查代码 / 配置 / 日志 / issue / PR / 运行结果、外部事实联网查可靠来源、未联网 / 未实测 / 未查到 / 无权限要标明、用户前提有误要指出;③ 分流缺环:只写"要引导用户 / 给选项",却没把导航 / 确认 / 局部补丁 / 高危闸落成可检查规则;④ 调研缺环:方案 / 设计 / 估算 / 选型 / 外部平台接入前没要求调研结论 / 主流做法 / 建议方案 / 信源凭据 / 未验证风险;⑤ 收口缺环:任务结束只说"完成了",不交代状态 / 验证 / 风险 / 推荐下一步。反向也查:把低风险局部补丁 / 纯解释 / 稳定常识都强制联网、确认、长卡片或假选择,同样不合理。
4. **结构缺失**:该档该有的(单一真相源/ADR/验收标准/review/说人话约束/立栈闸与 house_style 落点)缺了。**含 spec 交实现前没过独立新眼睛 review**(只审了代码 diff、漏 spec 本身 · exp-016)。
5. **凭据失真 / 交接断裂**:doc / issue / PR 分工不清(没有 doc 真相源、issue 替代正文、PR 只写 done 不写验证 / 链接 / 收口条件)、issue/PR 烂尾或状态与实际不符、issue 厚 doc 薄(细节堆 issue 难被 agent 复用,违 §1.8「issue 薄、doc 厚」);**issue 指向的 doc 在远端解析不开(悬空链接)、SOP 缺「生产先落 doc 再开 issue / 消费先验链接」的交接闸、或约束块缺「缺交付物反弹不自补」的角色边界 carve-out** → 凭据不可信、agent 会照空链接行动甚至自己伪造需求(§1.8 + §1.9)。**凭据书挡还查(所有项目)**:issue 堆着 `待业务确认` 却无「开工 surface 给业务 / owner」约定 / 收工不把收口 doc 推远端 = 等人拍板的决策烂在 issue、会话产物没落地 = 交接断裂。**还查协作总线断节**:方案 / 字段 / 范围变化只藏在 PR / commit / 私聊,不回写 issue 评论,后续 agent 接手就断上下文。**还查决策闸误关**:决策类 issue 把裁决当收口一关了之、下游验证(spike/数据)却没落定(closed 但关闭条件未满足)= 选路被当完成的说谎凭据 → SOP 缺「决策闸:裁决≠收口、写关闭条件 + 拦人误关」子规则(§1.8)。

输出:人读报告 + 可执行 findings(参照 `google-design-skill` 的 criticize 双轨)。

---

## 6. 验证试金石

本 STANDARD 与 `$sop-init` 不靠"看起来对"算数,**只有在一个真实多端多人项目(taoxi-geo)上跑通、生成物右尺寸且 owner 扫一眼 agent 指令文件 + 目录树即能判对,才算验透**。未验 = PPT。

> ✅ 已过:exp-003 在 taoxi-geo(多端多人)真跑通过;exp-004(geo-reverse)、exp-007(media-ops)真项目复验;exp-005/006 为本仓自验。exp-012 收掉档位枚举(本次)待复验。
