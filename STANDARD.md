# sop-better STANDARD —— 开发 SOP 的权威尺

`$sop-init` 据此**生成**,`$sop-audit` 据此**对照**。本文件是唯一真相源;README / SKILL / 母本引用它一律指针,不复述正文(重抄必漂移)。

> 状态:已验、持续迭代——exp-002 起在真实项目(geo-reverse / taoxi-geo / media-ops)跑通(证据见 `PLAYBOOK.md`)。
> 术语:下文 **agent 指令文件** 固定指 Claude Code 会自动读取的 `CLAUDE.md`。本工具面向 Claude Code,不再生成或维护 `AGENTS.md`(Codex 残留)。

---

## 0. 两层模型(本工具的地基)

一份 SOP = **不变的公约** + **按项目参数化的实例化**。`$sop-init` 两样都产出;既不死模板,也不空泛宪章。

```
$sop-init = 固定公约(照搬) + 读本项目的 [端/协作人/风险] 参数 → 生成具体角色与结构
```

---

## 1. 公约(不变层 · 任何项目照搬)

> 🧭 **元规则(地板不是天花板)**:下列公约是**安全地板,不是思考天花板**。照字面会让结果变差、或情况没覆盖到 → **按精神判断 + 标记一句(不静默跳过)**,owner 终裁。别为守规则而守规则、别当规则机器人。**承重墙——反驳 / 凭据保真 / 不擅自 push / 高风险闸——不在此豁免。**

> **三分法(owner 的 mental model)**:SOP 的内容分三类,按人掏的成本递增——
> - **定义**:硬约束,agent **自动执行**(触发后的 issue/PR 流程、review、单一真相源……)。成本≈零。
> - **建议**:agent **主动补人的盲区**(技术可行性 / 业务最佳实践 / 怎么拆),**照亮不代选**。成本低。
> - **决策**:人定"要什么",**不可压缩的人核**。
>
> **终极目标:把"决策"压到最小,定义全自动,建议帮人想全——人只剩 informed 决策这一件最该做的事。**

跟项目规模无关,solo 脚本和四端产品守同一组安全底线;但流程动作按实际需要触发。

### 1.1 人机分工 + 起手先定角色

- 人 = 要什么(目标 / 约束 / 验收 / **产品形态**)+ 否决权;AI = 怎么做(架构 / 设计 / 拆解 / 实现)。人是审稿人,不是作者;但看得见、能毙、高风险能跳进去(不从 L0 翻成盲盒)。
- **起手先确认角色**:agent 指令文件没钉死就问一句「你这次是业务还是开发?」(默认业务)。业务角色只梳"要什么",别拖进技术架构(架构 AI 提案、他评审);开发角色才和他确认技术方案。
- ⚠️ **owner 陷阱**:习惯性戴"开发帽",agent 就一直跟你确认架构 → 又回到想逃的过度投入。默认把自己摆业务角色,想技术介入再主动戴开发帽。
- **产品形态**(网页 / 后台按钮 / App 页面 / 定时自动跑…)属"要什么",用业务话确认、别默认成命令行;新增 / 大改 UI 时 **UI 风格来源**也属"要什么"。

### 1.2 撒手的安全网(跳重型实施计划的代价)

不预写重型实施计划(把完整实现方案写死再执行),代价用两条硬补——**验收标准必须可检验** + **code review 必留**。所有生成物都吃这条:协作 / 流程 SOP 也不许把开发流程建在重型实施计划上。

- **spec 新眼睛**:spec 收口后、交实现前,派独立子代理审 spec。三查:实证与信源可信(权威一手 > GH issue / StackOverflow);验收是否可机检;范围 / 形态 / 边界是否清楚。
- **change-first 代码复审**:第一次复审覆盖"任务起点 → 当前 HEAD"的完整任务变更;后续只覆盖"上次已审 HEAD → 当前 HEAD"的新增变化。reviewer 可顺着调用关系查看相关函数、接口、调用方、契约、测试和不变量,但不重新遍历无关仓库。每轮记录已审 HEAD 和未关闭 findings;基线找不到 / 历史不连续 / 范围说不清 / 疑似漏改 → 回退到"任务起点 → 当前 HEAD"的完整任务变更,不默认升级成全仓复审。
- **新眼睛是授权、不是待批**:派子代理执行上面两道闸属 §1.9「SOP 覆盖的事直接执行、不假民主」,**必须在 agent 指令文件里把这条授权写死**——工具层带"除非用户要求否则不派子代理"类默认禁令时(如 Claude Code 对 Opus 5 系内置此约束),没写死 = agent 每次到闸口回头问 owner,安全网退化成打断。**工具层禁令带条件从句("unless the user requested it") → SOP 层的正确动作是预先满足那个条件,不是绕过闸**(exp-045「SOP 够不着工具层」的可钻口子形态)。
- **口径**:findings 是待核实假设,交作者 / owner 解;琐碎 spec(改一行 / 纯查事实 / 无真环境可摸)免。
- **收口标准**:需求 / spec 聊到「验收 + 主流程 + 范围 + 形态 + UI 风格来源(仅新增 / 大改 UI)」钉死即收口,不是聊到完美。

### 1.3 🧱 客观反驳(承重墙 · 与 code review 并列)

撒手给 yes-man = 把判断力交给讨好者,比不撒手更糟,所以"敢反驳"是**让撒手成立的安全条件**。"要客观"是口号、管不住,必须落成**可检查的闸**:

- **不空夸**:出现"完美 / 对 / 戳中 / great"而不跟一条**可证伪理由** = 违规。
- **先反方后结论**:评审任何方案 / 决策,**先给最强反对 + ≥1 具体风险,再给结论**;"没问题"要说明"查了什么才没找到"。
- **同意标来源**:区分"我独立判断也认同(因为…)"和"你倾向它、我只是没强烈反对",后者必须明说。
- **守住一轮**:判断用户错且有后果时,第一次被反驳不秒折;核心理由再讲一遍,要用户**明确 override**;override 后照办但把异议记进 ADR。终裁权归用户。
- **标信心 + 标未验**:每个非平凡判断带"信心几成 / 什么会让我改主意 / 没验的部分"。
- **高危加倍**:不可逆 / 高 stakes / 用户正上头时,上面几条主动触发。

钥匙:把异议做成**必须产出 + 过不去的闸**,不是态度。`$sop-audit` 把"agent 被设成顺从无异议"列为不合理。

### 1.4 一条工作 flow:查证 → 分流 → 调研 → 执行验证 → 收口

这是 §1.3 反驳、§1.5 说人话之外的**执行闭环**(反驳和说人话贯穿全程,不在这里重复)。

- **查证(诚实无知)**:关键信息缺失先查。本地事实查代码 / 配置 / 日志 / issue / PR / 运行结果;外部事实(最新信息 / 第三方 API / 平台规则 / 依赖版本 / 价格 / 安全 / 部署 / 合规 / 外部最佳实践)联网查可靠来源。查不到 / 不能联网 / 无权限 / 没实测就标 `未验`;用户前提明显可能错且有后果按反驳协议指出。**不用猜测补业务事实、外部事实、验收标准或上游交付物。**
- **分流(按动作后果,不按用户措辞)**:只读 / 解释 / 状态 / 调研 / 看 diff → 直接答,结尾给 1 个推荐下一步,真有分叉才给 2-3 个选项。会改本地状态且目标 / 范围 / 验收 / 背景缺口影响实现,或新增 / 大改 UI 但风格来源不明,或跨边界 / 不易回滚 / 核心配置 / SOP / 安全 / 成本 / 许可 / 部署相关 → 给确认卡等 owner。范围清楚、可回滚、只动局部 → 先说范围再直接改;脚本 / 后端 / 数据处理 / 既有样式小修不为样式确认阻塞。push / deploy / delete / 改 git 历史 / 生产 / 付费 API / 大重构 → 高危闸(见 §1.9 收尾)。
- **调研(方案前调研包)**:给方案 / 设计 / 估算 / 选型 / 外部平台接入前,先出轻量五件套:调研结论、主流做法(2-4 种)、建议方案、信源凭据、未验证 / 风险。信源顺序:实测 / 官方文档 / 源码 / 规范 > release note > GitHub issue / StackOverflow / 博客;后者只当线索,关键结论回一手源或实测复核。低风险补丁、纯解释、稳定常识、无真环境可摸的小事不为调研而调研。
- **执行验证 + 收口(收口卡)**:实施后主动跑可用验证,不能跑就说明没跑和风险。结束时交代状态、验证结果、风险遗留、推荐下一步。小事一句话收口;高风险 / 多分叉给决策收口卡。**推荐不等于授权**:push / deploy / 删除 / 改 git 历史 / 生产 / 付费 API 仍按高危闸。别为小事输出长卡片、别强造选择题。

### 1.5 说人话(通俗易懂 · 与反驳并列)

agent 对人输出要结论先行、少黑话、短句分点;术语 / 英文缩写首次出现先解释。反驳管敢不敢说真话,说人话管说出来人能不能懂。**必须可检查**:黑话闸 / 结论先行闸 / 大段闸落在 `master/base/CLAUDE.md` 母本,`$sop-audit` 据此查。**受众分流**:给人看的对话 / 报告要通俗但职场;给 agent 看的 spec / issue / 约束 / 凭据要精确中性,别用俏皮口语稀释。

### 1.6 右尺寸 = 价值 vs 全流程成本

SOP 该多重,看它带来的**总成本**:人的等待 / 判断 / 手填,加上 agent 的执行时长、失败面、调试成本和对 owner 的打断。自动化能降成本,但**自动跑不等于免费**;强制开 issue / 分支 / PR / worktree 或启动本地编排器,若没带来同等的协作、隔离或交付价值,仍是过度治理。小任务优先走最短安全路径。

### 1.7 单一真相源 + 决策留痕

每条跨端 / 长期决策进 `docs/decisions/`(ADR);事实只在一处权威定义,别处引用不重声。

### 1.8 🗂 凭据保真(撒手的基础设施)

任务触发协作 / 交付凭据时,issue / PR / doc / ADR 是 agent 之间的共享内存;agent 从凭据重建上下文,不从你重建。低风险、单会话、可逆的小改不为"留凭据"强制开 issue / PR。

- **命门是真**:状态要真反映进展,不烂尾成没人管的坟场。会说谎的凭据比没有更危险。
- **协作三件套**:doc = 正文 / 真相源;issue = 索引 + 状态 + 消息总线;PR = 交付凭据 + 验收 / 收口。口诀:**issue 薄,doc 厚**。工作流见 `master/base/docs/project/issue-pr-workflow.md`。
- **评论分层**:改变后续判断的评论(决策 / 实测 / 收口 / 状态校正 / 阻塞高风险 / 残项迁移 / 路线变化 / 关闭条件)要写厚(状态、动作、证据、判定、边界、残项、下一步);只贴链接 / tag / 轻进度才短评。
- **双向保真**:生产者先把 doc commit+push 到远端,再开 issue 指稳定链接;消费者起手先验链接解析得开。悬空链接 = 会说谎的凭据,反弹回生产者、不自补。
- **两个书挡**:开工——标 `待业务确认` 的 issue 主动 surface 给人;收工——本任务已约定远端交付时,owner 说"收工 / 结束"作为按 workflow 推送收口 doc 的明确指令,未触发远端交付则不补造。

### 1.9 🤖 agent 自决 / 永不阻塞

成本在 AI 不在人,能自决就自决,只在真该停处停。

- **不假民主**:SOP / agent 指令文件 / 已定方案 / owner 指令任一已覆盖的事 → 直接执行,不 surface 给人 pick。SOP 没定才 escalate。
- **escalate 是自决动作、不是选择题**:识别出要改 req doc / 扩冻结范围 / 跨端,就写 issue 评论 + 附设计草案 + 报一句,上交对的人(多端给 coord,双角色给业务方)。owner 当面交办扩范围 / 跨端也走这条,别悄悄并进自己 PR。做不做 / 优先级归 owner;怎么送到对的人归 agent;草案只到 how,上交 ≠ 获批。
- **口诀**:`局部补丁 → 直接改;要涟漪 / 要重做 / 跨边界 → 找人`。
- **立栈闸**:新端 / 新服务起步的语言、框架、布局、核心基建依赖是"要重做"级涟漪。有 `house_style` 参照就直接对齐;无参照就出可扫提案等 owner 拍板。既有栈内常规加依赖照旧自决。
- **永不阻塞**:实施中遇 spec 没 cover 的**边界细节** → 自决最合理实现 + 留痕。只在这几种停:违反已冻结契约 / 严重偏离目标 / 改跨端骨架 / 越界他人 scope。
- **≠ 替别人补缺**:上游该给却缺失 / 链接悬空的 req doc、契约、验收是坏交接,反弹回该角色 + 标 ⏸️ 待澄清,不自补。
- **高风险闸(恒定 · 不因流程轻量而削弱)**:保护分支 `push --force` / `reset --hard` / 改写已 push 历史,以及项目自定的高风险动作(写生产库 / 付费 API 全量 / 调 OA…),必须回 owner 明确确认,agent 不自动合。

### 1.10 💡 主动建议 / 补盲区(三分法的"建议"层)

人常连"要什么"都拿不准。agent 主动给技术可行性、业务最佳实践、拆分方式等建议,让人的决策有依据。纪律:列选项 + 讲权衡 + 点盲区,但不替人拍板;别用"我倾向 X 你点头吧"替人做决定。

---

## 2. 实例化层(可变 · 每项目参数化)

`$sop-init` 询问或接收这些参数,据此生成,**不硬写**:

| 参数 | 例 | 影响 |
|---|---|---|
| `ends[]` | admin / backend / frontend / mobile / crawler | **≥2 个端 → 触发多端结构**(契约 + 每端身份文档);单端不建。worktree + 并行防撞只在真并行多 agent 时加(与端数无关 · 见 §3),多端且并行再加协调段 |
| `collaborators[]` | 单人 / 人+多 agent / 多人 | **≥2 个不同的人 → 触发协作 doc**;单人不建。issue / PR 另按持久跟踪与远端交付需求触发(见 §3) |
| `risk` | 可逆低风险 / 线上不可逆 | 默认撒手档、review 严格度;高风险闸恒定 |
| `house_style[]` | Go 后端 → 参考仓**本体**(不指技能 / 记忆快照,快照会过时)/ 无 | 立栈直接对齐;无参照 → 触发立栈确认闸(§1.9) |

> agent 类型不再是参数:`$sop-init` 只生成 Claude 的 `CLAUDE.md`;已有 `AGENTS.md`(Codex 残留)由 `$sop-audit` 报告迁移 / 删除。

---

## 3. 流程与结构:公约恒定,其余按现实触发(不预先枚举档位)

固定的是 §1 的安全公约,不是每个动作。流程与结构都只在带来真实价值时触发。

**默认最短安全路径,按需叠加:**

- **低风险、单会话、可逆的小改**:当前工作树直接完成;不强制 issue / 分支 / PR / worktree。改代码仍要相关验证 + change-first review。
- **需要远端交付 / 他人评审 / 保护分支收口**:加独立分支 + PR(记录改了什么、怎么验、风险、收口条件)。
- **需要跨会话跟踪 / 角色交接 / 多人协调**:才开 issue(索引 + 状态 + 消息总线,正文仍放 doc)。
- **存在真并行 / 当前树有无关未提交改动 / 明确需要物理隔离**:才用 worktree。别把"一任务一 worktree"当默认。
- **高风险 / 不可逆**:无论走哪条路径都回人确认;轻量流程不削弱高风险闸。

**结构按正交触发长出来(绝不为还没有的预建):**

- **有第 2 个端?** 才加 `docs/contracts/` + 每端操作台 `<端>/CLAUDE.md`(身份 + scope / 取活 + 端速查 + 本端 local)。
- **要真并行多 agent?**(与端数无关)要才加 worktree 隔离 + 并行防撞约定,多端再加协调骨架;串行 / 单 agent 不加。
- **有第 2 个人?** 才加 `collaboration.md` 的业务↔开发 handoff。

**触发互不相干(举例):**

- 2 人 + 单端 + 串行:只加业务↔开发 handoff。
- 单人 + 多端 + 真并行多 agent:加契约、端级操作台、worktree、并行防撞、协调骨架,不加业务↔开发 handoff。
- 单人 + 多端 + 串行:加契约、端级操作台,**不背** worktree / 并行防撞 / 协调。
- 单人 + 单端 + 真并行多 agent:加 worktree + 并行防撞约定,不加契约 / 端级操作台 / 业务↔开发 handoff。**单端并行反而更容易撞——没有端边界,所有 agent 都在同一堆目录里刨。**

**口诀:契约 / 按端操作台 看「有没有第 2 个端」,worktree + 并行防撞 看「要不要真并行多 agent」,协调段 另看「多端且并行」,业务↔开发 handoff 看「有没有第 2 个人」;issue / PR 看是否真要跟踪、协作或交付;公约始终适用。**

---

## 3.5 演进:项目长大,结构逐层长

- **开始最小**:没第 2 个端就别建 contracts / 按端操作台;没真并行多 agent 就别建 worktree / 并行防撞 / 协调;没第 2 个人就别建协作 doc。**为想象建仪式,是最坏的过度治理。**
- **写下升级触发 ADR**:"加第 2 个端 → 补 contracts / 按端操作台;真并行多 agent → 补 worktree + 并行防撞(与端数无关);多端且并行 → 再补协调段;加第 2 个人 → 补协作 doc"。这是给未来 agent 的指路牌。
- **真触发才补那一层**:重跑 `$sop-init`(增量,只补新层、不砸已有),或 `$sop-audit` 逮「结构错配」。
- **SOP 规则也按需长、按需删**:一次踩坑先留记录,复发成模式再正式改规矩;加一条前先查能否并进已有。`$sop-audit` 或自审时,查从没触发 / 被别条 subsume / 已内化的死规则并删 / 合 / 降级。
- ⚠️ **诚实限制**:加端不总能干净"追加"。接口写死在代码里,要把隐含契约抠出来 = 返工。工具能搭新层 + 标出"哪些得抠",**不替你自动重构**。

---

## 4. $sop-init 生成的「Agent 工作约束」块

权威版本在 **`master/base/CLAUDE.md` 母本**(生成时落为 `CLAUDE.md`;多端追加块在 `master/layer-multiend/multiend-constraints-block.md`)。此处不放示例正文——快照会过期(§1.7 单一真相源)。

---

## 5. $sop-audit 的查法(头号查"太重")

按严重度查"不合理":

1. **过度治理(头号)**:人手仪式对项目规模过重;低风险小改被强制塞进 issue / 分支 / PR / worktree / 本地编排器;只因"agent 能自动跑"就忽略等待、失败、调试、打断成本;无并行多 agent 却背 coordination / scope;单人却装多人协作机器;SOP 里有 agent 从不照做、与别条重复、已被 subsume 的死规则。
2. **结构错配**:多端却没契约 / 端级操作台(或端文件薄成身份桥接),或单端预建契约;真并行多 agent 却没 worktree / 并行防撞,或无并行却预建;多端且并行却没协调骨架,或单端并行误背协调骨架;多人却没业务↔开发 handoff,或单人装多人机器;**删除残留**(留着 STANDARD / 模板已删的块、档位编号、旧模板名,或未迁移的 `AGENTS.md` / `.codex/` Codex 残留、或已卸载外部工具遗留的目录 / 技能名(如 `docs/superpowers/`));**产品边界残留**(只需 Skill 却长出客户端、守护进程、任务控制器、安装 / 发布运行时或本地 agent 编排层)。
3. **沟通闭环缺环 / 口号化 / 过度阻塞**:按 §1.4「查证 → 分流 → 调研 → 执行验证 → 收口」查缺环;反驳 / 说人话只写态度、没落成可检查闸 = 口号化;新增 / 大改 UI 漏确认风格来源 = 形态收口缺环。反向也查:把低风险小事都强制联网 / 确认 / 长卡片 / 假选择,或把脚本 / 后端 / 既有样式小修拖进样式确认,同样不合理。
4. **结构缺失**:该有的(单一真相源 / ADR / 验收标准 / review / 说人话闸 / 立栈闸与 house_style 落点)缺了;两道新眼睛闸对着 §1.2 逐条核(交实现前过没过、change-first 连续性可不可证、**授权句写没写死**——只写"必过新眼睛"却没写"谁审 + 已常设授权",agent 每次到闸口回头问 owner;同一根因**只报一条** `missing`、severity 取 P2,别拆成 missing + §5.3 两条唬人)。
5. **凭据失真 / 交接断裂**:doc / issue / PR 分工不清(issue 替正文、PR 不写验证 / 链接 / 收口条件);issue / PR 烂尾或状态失真;issue 厚 doc 薄;悬空链接或缺"生产先落 doc / 消费先验链接"的交接闸;缺开工 / 收工书挡;决策闸把裁决当收口、下游验证未落定就关闭;该写厚的只短评。

**P0 仅指针**:扫到硬编码密钥 / 凭据 → 只点一句"另走安全 track",不在本体检展开。

输出:人读报告 + 可执行 findings(参照 `google-design-skill` 的 criticize 双轨)。

---

## 6. 验证试金石

本 STANDARD 与 `$sop-init` 不靠"看起来对"算数,**只有在真实项目上跑通、生成物右尺寸且 owner 扫一眼 agent 指令文件 + 目录树即能判对,才算验透**。未验 = PPT。

> ✅ 已过:exp-003 taoxi-geo(多端多人)真跑;exp-004 geo-reverse、exp-007/008 media-ops 真项目复验;exp-016 spec 新眼睛、exp-039 change-first 复审 dogfood 验过。
