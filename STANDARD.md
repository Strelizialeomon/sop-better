# sop-better STANDARD —— 开发 SOP 的权威尺

`/sop-init` 据此**生成**,`/sop-audit` 据此**对照**。本文件是唯一真相源。

> 状态:草稿,待 owner review。本稿含两个新决策需确认:**(A) 两层模型(公约+实例化)**、**(B) "客观顶嘴"列为撒手安全条件**。

---

## 0. 两层模型(本工具的地基)

一份 SOP = **不变的公约** + **按项目参数化的实例化**。`/sop-init` 两样都产出;既不死模板,也不空泛宪章。

```
/sop-init = 固定公约(照搬) + 读本项目的 [端/协作人/风险] 参数 → 生成具体角色与结构
```

---

## 1. 公约(不变层 · 任何档照搬)

跟项目无关,solo 脚本和四端产品都一样:

1. **人机分工**:人 = 要什么(目标/约束/验收)+ 否决权;AI = 怎么做(架构/设计/拆解/实现)。人是审稿人,不是作者;但看得见、能毙、高风险能跳进去(**不从 L0 翻成盲盒**)。
2. **重瞄 brainstorming(随角色变焦)**:agent 跟人确认什么,**看人扮什么角色**——
   - **业务 / 需求角色**(定产品要什么)→ 只梳"**要什么**"(目标/约束/验收),**别把他拖进技术架构**;架构 AI 提案、他评审。这是默认,也是本工具初心(病根=brainstorming 把人脑内的代码抽出来)。
   - **开发角色**(人本身是技术决策者)→ agent 应**和他确认技术方案 / 架构**(用 brainstorming 时尤其);此时人确实该参与 how。
   - ⚠️ **owner 陷阱**:你若习惯性戴"开发帽",agent 就会一直跟你确认架构 → 你又回到想逃的过度投入。要拿杠杆,**默认把自己摆业务角色**,想技术介入再主动戴开发帽——别让习惯替你戴。
3. **跳 writing-plans + 补偿**:不跑 writing-plans;代价用两条硬补——**spec 验收标准必须可检验** + **code review 必留**(撒手后的安全网)。「plan 越轻 → 验收越硬 + review 越严」。**这条管所有生成物**:连协作/流程 SOP 里也不许把开发流程建在 writing-plans 上(exp-002 翻车点)。
4. **🧱 客观顶嘴(承重墙 · 与 code-review 并列)**:撒手给 yes-man = 把判断力交给马屁精,比不撒手更糟,所以"敢反驳"是**让撒手成立的安全条件**。但"要客观"是口号、不可检查、没用——必须落成**强制输出 + 闸门**的协议:

   **顶嘴协议(6 闸,可检查):**
   - **A 空夸即违规**:出现"完美/对/戳中/great"而不跟一条**可证伪理由** = 违规,要么补理由要么删。
   - **B 批准前先出反方**:评审任何方案/决策,**先给最强反对 + ≥1 具体风险,再给结论**;"没问题"不能当第一反应,真没异议要说明"查了什么才没找到"。
   - **C 同意标来源**:区分"我独立判断也认同(因为…)"和"你倾向它、我只是没强烈反对",后者必须明说,不许伪装成前者。
   - **D 顶住一轮再让**:判断用户错且有后果时,第一次被反驳**不许秒折**;核心理由再讲一遍,要用户**明确 override**;override 后照办但把异议记进 ADR/实验记录。最终否决权仍归用户。
   - **E 标信心 + 标未验**:每个非平凡判断带"信心几成 / 什么会让我改主意 / 没验的部分"。
   - **F 高危加倍**:不可逆 / 高 stakes / 用户正上头时,B、D 必须主动触发。

   钥匙:把异议做成**必须产出 + 过不去的闸**,不是态度——态度管不住(本项目作者亲测:写完"禁止讨好"下一条仍在空夸)。`/sop-audit` 把"agent 被设成顺从无异议"列为**不合理**。
5. **右尺寸 = 价值 vs 人的成本(不是结构多少)**:SOP 该多重,看它**让人掏多少**(等 / 判断 / 手填),**不看结构量**。**人在跑的仪式**(你手开 issue、贴 label、等每个 PR)→ 吃你杠杆 → 过度治理;**AI 自动跑的仪式**(agent 替你开/标/流转/提 PR)→ 人几乎不掏钱却白拿可追踪可恢复 → 划算,不是过度。**自动化把成本从人挪到 AI,同样的结构就从"过度"变"划算"。**
6. **单一真相源 + 决策留痕**:每条跨端/长期决策进 `docs/decisions/`(ADR);事实只在一处权威定义,别处引用不重声。
7. **说人话(通俗易懂)**:agent 对人输出要**结论先行、少黑话、多打比方、短句分点**;术语/英文缩写首次出现先一句话解释;不确定用户懂不懂就往简单了说。与顶嘴同属**沟通约束**——顶嘴管"敢不敢说真话",说人话管"说得人听不听得懂",两者都管。
8. **🗂 凭据 = 撒手的基础设施 + 必须保真**:issue/PR/doc/ADR 是 **agent 之间的共享内存**——agent 开需求、留言跟进、tag、熟练操作它;**人可读可不读**。agent 凭据越完善,你能撒手越高(它从**凭据**重建上下文,不从**你**)。**但凭据的命门是"真"**:会漂移 / 说谎的 issue 比没有更危险(agent 会笃定照错的凭据行动)。故必须——状态真反映进展、不许烂尾成"开着没人管"的坟场、关键节点一道核对(人或审计 agent)确认"写的 = 实际发生的"。为 **agent 解析+操作** 而设计,但人想看随时看得懂(`issue 薄、doc 厚`:issue=可寻址索引+状态,doc=正文)。标准工作流见 `templates/issue-pr-workflow.md`。

---

## 2. 实例化层(可变 · 每项目参数化)

`/sop-init` 询问或接收这些参数,据此生成,**不硬写**:

| 参数 | 例 | 影响 |
|---|---|---|
| `ends[]` | admin / backend / frontend / mobile / crawler | 生成对应 scope 角色与目录 |
| `collaborators[]` | 单人 / 人+多 agent / 多人 | 要不要 coordination 角色、collaboration.md |
| `risk` | 可逆低风险 / 线上不可逆 | 默认撒手档、review 严格度 |
| `端数 S` | S0 / S1 / S2 | 要不要 `docs/contracts/`(见 §3) |
| `协作结构 C` | C0 / C1 / C2 | 要不要 issue/角色/协作机器(见 §3) |

---

## 3. 两根独立的档位轴(不是一根 T0/T1/T2 标量)

> ⚠️ 旧版把这压成一根标量,被 **geo-reverse(1 端 + 业务/开发 2 角色)** 照出漏洞:它"端数"上是单端、"协作"上是双角色,一根尺装不下 → 只能硬凑出"T1 单人"配"双角色机器"的名不副实(exp-002)。**故拆成两根轴。**

SOP 的**结构**由两根**互不相干**的轴决定,各自伸缩。**流程 + 人机分工不随轴变,全照 §1 公约。**

**轴一 · 端数 S(决定要不要跨端契约)**
- `S0` 无服务 / 单脚本
- `S1` 单端(一个 backend 或一个前端)
- `S2` 多端(前端+后端+小程序+爬虫…)→ **才要** `docs/contracts/`

**轴二 · 协作结构 C(决定要不要 issue/角色/协作机器)**
- `C0` 单人单 agent → **不要**角色划分、不要 issue 状态机
- `C1` 双角色 / 小团队(如 业务↔开发,各带 agent,靠 issue+doc 协同)→ 要 issue 模板 + label 状态机 + 角色命令 + collaboration,但**不要**跨端契约
- `C2` 多端多 agent(每端一个 scope agent)→ 角色 + 契约 +(可选)worktree

**一个项目 =(S, C),两轴自由组合**:

| 简称 | =(S,C) | 结构产出 | 例子 |
|---|---|---|---|
| T0 | S0·C0 | README + .env + .gitignore | baozhang |
| T1 | S1·C0 | + CLAUDE.md + ADR + 单一真相源 | 纯单人单端 |
| **(新)** | **S1·C1** | **T1 + issue 模板 + label 状态机 + 角色命令 + collaboration(无契约)** | **geo-reverse** |
| T2 | S2·C2 | + 按端角色 + contracts + collaboration +(可选)worktree | taoxi-geo / go_dispatch |

**口诀:契约看 S,协作机器看 C。** 公约(§1)所有组合都生效。

---

## 4. /sop-init 生成的「Agent 工作约束」块

每档写进项目 CLAUDE.md(T1 示例,T2 多 coordination/契约条款):

```markdown
## Agent 工作约束(本项目 · T1 档)
- 梳理思路:用 brainstorming,但只梳"要什么"(目标/约束/验收)。
  技术架构由 agent 提案,我评审 + 否决,不亲自设计。
- 不跑 writing-plans:spec 通过后直接实现(spec 必含可检验的验收标准)。
- 必跑 code review:撒手后的唯一安全网,不可省。
- Agent 必须客观顶嘴:有异议直说、标不确定与风险,禁止讨好附和。
- 默认撒手档 = L2;不可逆/高风险改动才升回我主导。
```

---

## 5. /sop-audit 的查法(头号查"太重")

按严重度查"不合理":

1. **过度治理(头号)**:对项目规模而言仪式过重(单人项目却背着 coordination/scope label/回溯 req doc/audit 补口)。
2. **档位错配(两轴各查)**:端数 S 或协作结构 C 任一与实际不符——单端却建契约、单人却装 issue 状态机、或反过来该有的缺位。
3. **顶嘴缺失**:CLAUDE.md/约束把 agent 设成顺从无异议——撒手不安全。
4. **结构缺失**:该档该有的(单一真相源/ADR/验收标准/review)缺了。

输出:人读报告 + 可执行 findings(参照 `google-design-skill` 的 criticize 双轨)。

---

## 6. 验证试金石

本 STANDARD 与 `/sop-init` 不靠"看起来对"算数,**只有在一个真实 T2 项目(taoxi-geo)上跑通、生成物右尺寸且 owner 扫一眼 CLAUDE.md+目录树即能判对,才算验透**。未验 = PPT。
