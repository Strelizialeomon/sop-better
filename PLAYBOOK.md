# PLAYBOOK · 撒手操作手册

实验攒出来的**结晶**。一句一句长。

这才是"固化好 SOP"——但固化的是**用证据换来的新打法**,不是那台旧控制机器。

> ⚠️ 规矩:这里每一条都必须**有实验编号背书**。不许写"我觉得应该……"。没跑过实验、没有数据的,不准进来。

---

## 条目格式

```
### [能/不能] 在 L{档} 把【某类活】交出去
- **前提**:什么条件下成立(尤其是"怎么便宜验证")
- **证据**:exp-NNN, exp-NNN
- **代价 / 翻车点**:什么时候会塌
```

---

## 已沉淀的条目

### ✅ 能在 L2 把【全新、低风险、聊透了的项目设计】交出去
- **前提**:AI 把**每个决策 + 一句理由列成可扫清单**,让你 2 分钟审完它的脑子、逐条能否决 → 这是"便宜验证"在设计层的形态。且任务爆炸半径小(全新项目、全是可改 markdown)。
- **证据**:exp-001(设计 sop-better 本身)
- **代价**:盲盒感 → **把控感下降**。owner 实测后判定:这种低风险场景下可接受,值。
- **翻车点(待更高 stakes 验证)**:在 owner taste 极强、或不可逆的高风险场景,放弃把控的代价会陡增,届时应降档。

### ⚖️ 原则:撒手档位由 (风险 × 验证成本) 决定,不由"信仰"决定
- "永远 L0 自己扛"和"永远 L4 盲盒"都是不动脑。技能是**校准**。
- 低风险 + 可逆 + 验证便宜 → 大胆往高档撒手(如 exp-001)。
- 高风险 + 不可逆 + 验证贵 → 降档,先把验证做便宜再说。
- 把控感是用来**换杠杆**的,换得值才换。
- **证据**:exp-001(给"拥抱未来"的冲动配的护栏)

---

### ⚠️ L3 把「按 STANDARD 实现工具」交出去:骨架对,但 agent 会拿你别的项目的习惯填空
- agent 遇到 STANDARD 没写死的地方,会**凭对你其它项目(taoxi-geo)的记忆**补 → 把旧习惯(writing-plans)偷带进来,跟新主张自相矛盾。
- **护栏**:STANDARD 关键约束写死、别留模糊;skill 明令「用 templates,别凭记忆另写协作文档」。
- **证据**:exp-002(geo-reverse 真跑)

### ⚖️ 真跑 > dry-run:对一遍结构看不出的矛盾,真跑一次就现形
- 原计划只做「结构 dry-run」验,owner 直接真跑 geo-reverse,当场撞出 2 个 bug(writing-plans 矛盾、档位 1 维)。
- **教训**:工具类验证宁可在一个可丢弃的真项目上跑一遍,别只在脑子里对结构。
- **证据**:exp-002

---

### ✅ "人的成本 / 凭据保真"重定义,上真项目立刻见效
- exp-003 在 taoxi-geo(S2·C2)真跑:audit **精细不粗暴**——没乱砍正当的 contracts/worktree,把 820 行冗长当"信号"而非"罪证",还主动 deferred 防 merge 冲突。
- ⭐ 刚加的 §1.8「凭据保真」**第一次接触真项目就逮到一条真烂凭据**(principles.md 过时、自相矛盾)。说明"右尺寸=人的成本 + 凭据要真"这轮重定义是对的、可操作的。
- **证据**:exp-003 / taoxi-geo PR #179

### ✅ audit 默认只读、owner 说"改"才链改;砍掉独立 /sop-improve
- taoxi-geo PR #179 暴露:audit 一被授权就改文件 + 开 PR。**决策(owner 拍)**:扶正成设计——**默认只出报告,owner 明确说"改/go"才走 issue/PR 工作流修复**。
- 既然 audit 点头就改,独立 `/sop-improve` 跟它撞车、没剩多少事 → **砍掉**(右尺寸:不为没出现的需求养命令)。剩两命令:init + audit。
- **证据**:exp-003 / taoxi-geo PR #179

---

### ⚠️ 撒手给开发 agent:上游缺交付物时,它会把「永不阻塞/补盲区」误读成「自己补别人的需求 doc」
- agent 拿到 issue、发现关联 req doc 在远端不存在(业务"提 issue"和"推 doc"分两步、漏了推),会**凭「永不阻塞 + 主动补盲区」自己把需求 doc 写了** → 越界伪造真相源(比阻塞更糟:照一份没人认的"需求"笃定行动)。SOP 当时不仅没拦,**其最强原则正指向这个错误动作**——agent 是忠实执行,不是违反。
- **护栏**:①「自决/永不阻塞」加角色边界 carve-out(别人该给而缺/悬空的交付物 = 坏交接 → 反弹不自补);②「凭据保真」做成**双向**(生产先落 doc 再开 issue·指稳定引用 / 消费起手先验链接解析得开);③悬空链接 = 会说谎的凭据,`/sop-audit` 列为 finding「凭据失真 / 交接断裂」。
- **代价 / 边界**:这是**一次**事故就改了 STANDARD(顶 §3.5"复发 2+ 次才改规矩")——因为它是**无边界规则主动导致越权的设计缺陷**,非调参数据点;且修的代价(一句 carve-out)远小于复发代价(伪造需求)。**同类"一次就改"要过这两关:设计缺陷 + 代价不对称。**
- **证据**:exp-004(geo-reverse issue #2 真跑 + geo-reverse/media-ops 2 项目 audit 复验:同一 finding 2/2 命中,坐实系统性缺口)

---

### ⚠️ 给「反过度治理的工具」加规则:默认草案会犯它自己治的病(重抄已有 + 为小缺口立大概念)
- 给 SOP 加业务侧"开工/收工"动作,agent 第一版草案就过度治理:立新公约 §1.11 + "闭环"品牌 + 穿 7 文件各加一段 + 重抄 4 条已存在的规则(sync/验链接/doc 先推后开 issue/自检保真)。**真缺口其实只有两点**,其余全可指针引用。
- **护栏**:①改 SOP 前用 `/sop-audit` 的镜头审自己的提案(这条是不是已在别处定义?能不能指针接上?);②spec 必带**反向验收**——grep 确认"没加不该加的"(新章节/新概念/重抄正文);③**净增行数硬上限**(本次 ≤25,实际 6),超了即"加功能悄悄变加仪式",回炉。
- **证据**:exp-005(业务会话闭环 · 瘦身版)

### ✅ 反驳闸是双向的:owner 一句质疑能在 spec 前拦下过度治理,比事后 audit 便宜
- 本次过度治理不是 agent 自己发现的,是 **owner 一句"这是否违反 better lighter 初衷"**在写 spec 前拦下的;agent 据此重审、认罪、砍到 ~1/3。
- **教训**:§1.4 客观反驳不只管"agent 敢不敢顶 owner";**owner 对 agent 草案的一句质疑同样是安全网**,且越早(spec 前)越便宜。agent 收到这种质疑要真重审、别辩护。
- **证据**:exp-005

---

### ⚠️ 非技术需求方会被"开发顺手的默认形态"带偏——收口标准必须把『产品形态』列为业务要确认的一项
- geo-reverse #2:不懂技术的需求方最后"确认"的需求是**命令行(CLI)**——不是他想要 CLI,是 agent 梳"要什么"时**没把"产品长什么样"用业务话摆出来让他选**,形态这个空位被开发顺手的默认占了。**产品形态(网页 / 后台按钮 / App / 定时任务)属"要什么"、该业务定,不是"怎么做"的技术架构。**
- **护栏**:①STANDARD §1.2 点明形态属"要什么"、必须用业务话确认、别默认 CLI;②收口标准 三样→**四样**(验收 + 主流程 + 范围 + 形态);③约束块业务角色行 + C1/C2 的 req-doc 内容都带上形态。
- **代价 / 边界**:又是"**一次踩坑就改 STANDARD**"(顶 §3.5「复发 2+ 次才改规矩」)——理由同 exp-004:这是"收口标准漏了一个维度 → 做错产品"的**设计缺陷**,且**代价不对称**(清单加一格 vs 整个产品白做)。"一次就改"的两关(设计缺陷 + 代价不对称)都过。
- **配套**:开发侧补"取活先判要不要细化、缺『要什么』反弹业务"——和 exp-004 的 carve-out 同源(#2 的另一面:开发自补了需求)。
- **证据**:exp-004(geo-reverse #2)

---

### ⚠️ 防 SOP 僵化:僵化源是 always-loaded 的"体量",不是单条规则对错
- owner 担心规则越攒越多压垮 agent 想象力。实测:真病灶是 **always-loaded 约束块 10 条 / 1330 字**(每 token 怼脸 → 打勾心态),不是 STANDARD 总行数。多数规则其实是**"许可证"**(扩 agent 能动性:自决/反驳/不阻塞),不是"镣铐"——SOP 故意不碰解法,想象力活在解法里。
- **治法(三件 · 都不是"删功能 / 少迭代")**:① **元规则**"地板不是天花板,按精神判 + 标记"(发想象力许可);② **反向齿轮**(STANDARD §3.5 + audit):规则也按需删/合/降级,SOP 允许掉秤——治"只会加挡→攒成笼子";③ **瘦内核**:情景规则从 always-loaded 搬去 on-demand 文档(用"触发时刻"指针),约束块 10 条→5 条 + 1 指针(1330→760 字,net −3 行,真减不丢功能)。
- **证据**:exp-006

### ⚠️ 减法 / 搬迁规则,比加法更要新眼睛——最容易在"摘要版"里丢掉安全护栏
- 瘦内核时把"承重墙(反驳/凭据保真/不擅自 push/高风险闸)不豁免"这条例外漏在了 STANDARD、没带进 always-loaded 的内核;agent 读的是内核 → "按精神判"会被开后门。新眼睛逮到补回。同类:收工摘要省掉"main 受保护则 PR"会诱导硬推保护分支。
- **护栏**:压缩 / 搬迁规则后,逐个核**安全护栏有没有跟着搬全**(尤其 always-loaded 摘要版);别让"摘要"制造读错捷径。
- **证据**:exp-006

---

### ⚠️ 多端(S2)缺"端角色文档"→ 各端不知自己是谁 → 交接乱套;蒸馏母本时把这层载重结构漏了
- go_dispatch 反例:一条 backend 端内 spec 在主仓(coordination 座位)起头,agent 不知自己该是哪个端 / 该在哪干,凭空造出"释放分支 + 跨端交接"一通乱,被 owner 纠正 4 次。根因:**多端身份没有"自动加载"的落点**——sop-better 只生成项目级 CLAUDE.md + collaboration 角色表(要 agent 主动读才生效),缺**每端一份自动加载的 `<端>/CLAUDE.md`**。
- 母本(taoxi-geo + go_dispatch)各自独立长出"root + 每端 CLAUDE.md"三层身份栈;sop-better 蒸馏时只搬了 root〔A〕+ collaboration〔B〕,**漏了 Layer C**——而 SKILL.md §S2 还留着"按 ends[] 的端角色"字样却**无模板兜底**(→ 要么静默跳过,要么违反"不许凭记忆另写"的禁止条)。
- **护栏**:①新 `templates/end-role-claude.md`(端级身份:身份 + 本端 local〔scope/技术栈/常读/实施层词汇〕+ 指向 §10)· `/sop-init` 在 **S2** 按 `ends[]` 每端生成一份;②靠 **harness 自动加载 cwd 最近 CLAUDE.md** = 进端即定身份(**必须叫 `CLAUDE.md`**,换名即废);③纪律:**端文件指针不复述通用红线**(否则成漂移源 · §1.8)——这点 go_dispatch 已比 taoxi-geo 优(把通用红线收成一行 §10 指针),照它;④"错座位护栏"并入 collaboration.md 多端追加段(端内活归 scope agent · 已产在主仓则走 req-doc 交接,别"释放分支")· ⑤身份分层**不写进 always-loaded 内核**(机制靠自动加载自跑 · 守住瘦内核 · better-lighter 自审时把首版多加的那行砍回)。
- **边界**:仅 S2(单端没有"端")· 门槛同 worktree(真并行多 agent 才值)。
- **元教训**:**蒸馏会丢"载重结构",不只是修剪肥肉**——两个母本独立收敛到的结构,瘦版漏掉就是信号、不是巧合;改 SKILL 时"留了名却没留模板"= 半截缺口,比全无更坑。
- **证据**:go_dispatch 反例(2026-06-08 会话)

---

### ⚠️ 决策闸 issue 不能「裁决即关」——选了路 ≠ 收了口,下游验证没落定前关 = 说谎凭据
- media-ops #25(标题自带"P0 私信路线闸"):业务方拍了 A 路(专业号/KOS 够)就手点关成 COMPLETED,但这条路到底能不能走,还压在一个**没跑完的 spike**(pro 私信通能不能稳定读+回)上。**关它是合 SOP 的、不是手滑**——media-ops 工作流写着"决策类 issue:裁决落 ADR + 关闭即完成",这条把所有决策当"拍了就完",没有"决策闸=裁决后还要等验证才收口"的子类 → 把人领向提前关(同 exp-004 形状:最强规则指向错误动作、执行者忠实执行)。三道本该兜底的全没接住:决策类规则本身、关前自检(只查 label 卫生)、业务收件箱(催人去拍、拍完最自然就是关);且决策 issue"无验收环节"→ 退出条件只活在正文散文里,误关后无法机检。
- **护栏**:①工作流「关」步加**决策闸 carve-out**(裁决≠收口:正文写关闭条件 + 一句「只回复决策、别关 issue,关闭由开发按条件统一做」+ 裁决后 label 不进终态、挂 spike 上);②STANDARD §5 + sop-audit §4「凭据失真」加查"决策闸被裁决即关"(closed 但关闭条件未满足);③关闭条件写成可机检 → audit/sync agent 能逮误关自动重开,不靠 owner 碰巧扫到(#25 这次就是 owner 碰巧扫到才 reopen)。
- **代价 / 边界**:又是"**一次就改**"(顶 §3.5「复发 2+ 次才改规矩」),过两关——**设计缺陷**:"裁决即关"构造上装不下"闸"、还把人导向误关(可证、不靠 N);**代价不对称**:规则加一格子类 vs 一条 P0 路线被假关、下游当 A 已坐实往前走。仅适用**决策闸**(裁决选了路、且有下游 spike/数据 gating);纯裁决(拍完即定,如选型)照旧"裁决即关",别滥扩成"所有决策都别关"。
- **复验(诚实)**:目前仅 media-ops 1 例 + 规则本身可证指错(设计缺陷不靠 N 成立);**未做第 2 项目 audit 复验**(顶 exp-004 的 2/2)。待补:对另一 C≥C1 项目跑 audit 看决策闸有没有同样被"裁决即关",凑 2/2 才算坐实系统性。
- **证据**:exp-007(media-ops #25 真事故 + sop-audit 复验其工作流"决策类即关"规则指错)

---

### ⚠️ STANDARD/template 学会一条规则 ≠ 已生成的项目跟着学会——不回灌,lab 会被同一条旧规则再咬
- **back-port 缺口**:决策闸 carve-out 早进了 `templates/issue-pr-workflow.md:23`(exp-007),但 media-ops 自己的 `docs/project/issue-pr-workflow.md:44` 还是旧规则「裁决落 ADR + 关闭即完成」——**没回灌**。结果 **#29(抖音决策)被同一条没修的旧规则又误关一次**(裁决即 `Closes`、ADR-0006 仍"提议"、4 项开发评估全空、0 评论)。**修复活在模板里、没落到项目里 = STANDARD §6「未验=PPT」的同形**:没回灌的修复 = PPT 修复。
- **也是 exp-007 的同项目复发**(#25 之后又出 #29):证实「裁决即关」只要不修就反复指错;但**跨项目 2/2(geo-reverse)仍未补**,系统性结论照旧待坐实(诚实)。
- **护栏**:① `/sop-audit` 流程 §3 加「**模板版本差**」比对——项目治理 doc 跟当前 `templates/` 比,「项目缺、template 有」的新护栏 = 漂移根因 → 回灌(揪根因、不只揪症状);② **回灌纪律**:STANDARD/模板长出新护栏后,主动对所有活着的 lab 项目跑一遍 audit + 回灌,别等它再咬。
- **代价 / 边界**:回灌**不是无脑同步**——项目可能有正当的本地偏离(右尺寸);audit 比的是「缺了该有的新护栏」,不是「跟模板逐字一致」,别把合理差异报成漂移。
- **证据**:sop-audit media-ops 2026-06-09(逮到 #29 · exp-007 续)

---

### ⚠️ "怎么做归 AI"不等于立栈也自决——owner 的 house-style 是从没人问过的参数,缺它 agent 会忠实地裸写
- media-ops #60:**方向级**路线有闸(ADR-0008 + spike #58),但**实现级**技术路线(框架/布局/核心依赖)agent 按「能自决就自决、别假民主」裸写 stdlib;owner 脑中其实有 go_dispatch house-style,但 sop-init 只问 S/C/ends/risk、从没问过 → #62 反向 ~900 行重铺。规则又一次指错(exp-004/007 同形),且"确认技术方案"挂在 brainstorming 名下、从 issue 直接实现的路径上不触发。
- **护栏**:① sop-init 问 `house_style[]`(指参考仓**本体**、不指技能/记忆快照——"hi_gin 已过时"为证;owner 确认、不静默凭记忆填);② 自决口诀加 carve-out:**立栈**(新端/新服务起步的基建选型)= "要重做"级涟漪——有参照直接对齐(定义层零成本),无参照出可扫提案(每决策+一句理由)**阻塞等拍板**;③ 边界:既有栈内常规加依赖照旧自决,防闸变仪式;④ audit §5.4 查"立栈闸 + house_style 落点"缺位。
- **代价 / 边界**:又是"**一次就改**",过两关——**设计缺陷**(自决条款可证指向"自己选栈" + 参数表结构性缺位,不靠 N)+ **代价不对称**(规则净增 ~4 行 vs 每次立栈 ~900 行重铺风险)。跨项目复验待补(回灌时看 taoxi-geo / geo-reverse 同缺否);"agent 能否识别立栈时点"待 media-ops D2+ 真跑验。
- **证据**:exp-008(media-ops #60→#62 真事故)

---

### ⚠️ escalate 是 agent 自决动作、不是 owner 选择题——方向判对了还会"多问一步"把上交也做成 pick
- taoxi-geo #206:owner 当面给 admin agent 一个**冻结范围外 + 跨端**的需求。agent **方向判对了**(认出"不能自己吞、要回 coord 改 req doc"),却把 **escalate 这个动作本身**做成 owner 选择题("选项 1 现在并进 / 选项 2 fast-follow……你选哪个"),**逼 owner 回一句"去写评论"才动手**。可"写评论上交 coord"按 SOP(§1.9 不假民主 + collaboration.md 多端追加段 scope隔离)本就是自决该做的 → 多问一步 = 假民主真 ask,且**直接吃掉撒手杠杆**(owner 得手把手驱动每次跨端交接)。根因:§1.9「不假民主」撞上 §1.1「owner 定要什么」边界没划线,agent 把"机械上交动作"误并进"产品决策"(exp-004/007/008 同形:最强规则指向"让 owner 拍")。
- **拆包(护栏核心)**:**做不做 / 优先级 = owner 的产品决策(§1.1);怎么把它正确送到对的人手里(C2=coord / C1=业务方)= agent 自决动作**——自己写 issue 评论 + 附设计草案 + 报一句,别请示"要不要 / 怎么 escalate"。**owner 当面交办的扩范围/跨端需求同样走这条**:别因"老板亲口说"就吞进自己的活。
- **护栏**:① `STANDARD.md §1.9` 加「🔀 escalate 是自决动作、不是选择题」carve-out(端-agnostic 真相源);② `collaboration.md`(多端追加段)scope隔离 加多端落地(指针引 §1.9,不重抄);③ **不动** always-loaded 内核(靠现成「跨端 → 读 collaboration」指针路由 · exp-006 瘦内核)+ 不动 audit(运行时洞,传播靠模板版本差 · exp-007)。
- **代价 / 边界**:又是"**一次就改**",过两关——**设计缺陷**(§1.9/§1.1 边界未划线,可证指错)+ **代价不对称**(规则净增 ~4 行 vs 每次跨端 escalate 都要 owner 手把手 = defeat 撒手杠杆本身)。**比 exp-004 弱**(这次只多问一步、没伪造需求)——升级靠"多问一步直接 defeat 撒手杠杆 + 修复极便宜",不靠事故严重度。跨项目复验 + C1 落点待补(见 exp-009 §6)。
- **证据**:exp-009(taoxi-geo #206 真事故)

---

### ⚠️ audit 的「模板版本差」会被"近似重复"骗过——项目有大原则近似版、缺要命那一刀,被防过度报护栏豁免放过
- **dogfood 证伪**:exp-009 §6 预言"项目缺 escalate carve-out = 模板版本差自动逮 → 回灌";owner 真在 taoxi-geo 跑 `/sop-audit`,**没逮到**。根因:taoxi-geo 已有 §1.9 的本地近似版(`:635`「SOP 没定 → escalate flow、别让 owner pick」+ `:643`「跨端 feat → escalate coord」),模板版本差 check 那条"别把合理差异报成漂移"的护栏把它判成"已覆盖"。但近似版把问题框成"SOP 定没定",**缺的正是 exp-009 那一刀**(owner 当面亲口交办同样别吞、别把 escalate 做回 pick)——#206 事故恰栽这。**"近似版缺要命 delta" ≠ 合理本地偏离**,却被豁免静默放过。**audit 自己版的 exp-004/007/008/009**(最强护栏指向错误动作:防 cry wolf 反把真缺口放过)。
- **护栏**:模板版本差 check 加「近似 ≠ 覆盖」收窄——**逐条语义比**:看 template 那条有没有针对某具体场景 / 边界的限定(carve-out / 反例 / "即使 X 也要 Y"式例外),项目近似版只搬大原则、漏了那个限定 = 仍 `missing` + **强制标信心**;普通措辞 / 详略差异照旧宽放(不破头号防过度治理)。**判据靠语义、不靠字面标记**:首版押"带 `carve-out`/exp-NNN 标记才较真",新眼睛 grep 打穿(`exp-NNN` 在 templates/ 零命中、`carve-out` 9 选 2、连该逮的"验链接"护栏都没标)——模板刻意干净、字面锚点覆盖不全,只能靠 LLM 语义读。
- **代价 / 边界**:又"**一次就改**",过两关——**设计缺陷**("有/无"粒度 + "别报合理差异"护栏**构造上**装不下"近似版缺 delta",可证)+ **代价不对称**(回灌纪律整条压在 audit 能逮漂移上 vs SKILL 净增 1 句)。**单点 + 改的是 audit 自己 → over-flag 风险真实**:故强制标信心 + 明写"别扩成事事都报";**若复用中开始为正当偏离凑数报 delta,回炉收窄或撤**。复验待补:修完**重跑 taoxi-geo audit** 验逮得到(直接回归)+ 跨项目验噪音。
- **证据**:exp-010(dogfood:owner 跑 taoxi-geo `/sop-audit` 漏报 exp-009 回灌 · exp-009 §6 预言被证伪)

---

### ⚠️ 「说人话」长期是半成品公约——约束块许诺"可检查的闸",却只对反驳兑现、对说人话留软口号
- **半成品**:沟通约束块块头承诺「"要说人话"是口号、下面是**可检查的闸,过不去不许放行**」,但**反驳**半边落了 6 条可逐条判违规的硬闸、**说人话**半边只有 4 条软口号(少黑话/往简单了说)——没触发点、没"= 违规"。同 `§1.4`「口号管不住、必须上闸」教训的**同形漏网**(反驳照做生效,说人话从没拉到这条线)。owner 实测多项目对人输出不通俗(满嘴术语 + 大段没重点),正是那几条点了名却没上闸的事。
- **护栏**:① 模板说人话小节 4 软口号 → **4 行为级硬闸**(黑话闸/结论先行闸/大段闸 + 受众 carve-out);② `STANDARD §1.7` 加指针(说人话同反驳必须上闸、权威闸在模板);③ `§5.3` `反驳缺失` 扩成 `沟通约束塌成口号(反驳/说人话)`——audit 也查"说人话没上闸",**够到已存在的项目、不只管新建**。
- **张力(怎么改不变规则机器人)**:闸**行为级、不写数字阈值**(死阈值逼 agent 切碎闲聊 → 撞元规则);**受众 carve-out** 兜住——三闸只管「给人看的」(对话/报告),agent-facing 的 spec/issue/约束/凭据保持精确密度、不许借"说人话"稀释真相源。
- **代价 / 边界**:又"**一次就改**",过两关——**设计缺陷**(约束块块头自相矛盾,可证、不靠 N)+ **代价不对称**(模板净增≈0 vs 每个用本 SOP 的项目继续背叛自己的说人话公约)。**比 008/009 弱**:证据是 owner 观察 + 结构矛盾论证、非带 issue 号实锤事故;**over-mechanization 风险真实**(agent 可能变碎句),靠 carve-out + 元规则兜,**变机器人就回炉收软**。复验待补:**真跑 lab 项目需求沟通**验是否真变通俗(owner 原始诉求终验)+ 回灌 3 个 lab。
- **证据**:exp-011(owner 观察多项目对人不通俗 + 约束块"许诺闸却没上闸"自相矛盾)

---

### ⚠️ 合并/收编护栏时,"合进同一个文件" ≠ "共用同一个触发闸"——挂错闸 = 对"有此触发无彼触发"的项目类静默失效
- **根因**:收档位枚举时把 `collaboration-c2`(触发=**多端 + 真并行多 agent**,与人数无关)合进 `collaboration.md`(触发=**多人**)。两套护栏(6+1/消息总线/scope 隔离/coordination)文字一字没丢,但**整份文件挂到了"第 2 个人"门后** → 一个**单人 · 多端 · 真并行多 agent**项目(= taoxi-geo 自己的形态)拿不到它。`grep 护栏在场` 自审查得出"文字还在",**查不出"挂错闸 → 对某类项目够不着"**。
- **怎么暴露的**:只有**独立新眼睛 review 拿每条被合并的护栏去撞一个真实项目类**(它命中触发 A〔多端并行 agent〕但不命中触发 B〔多人〕)才现形;agent 自审(reverse-acceptance grep)结构上盲这一类。
- **护栏**:① 修复——多 agent 协调骨架改挂「真并行多 agent」(= worktree 同闸),与"多人 handoff"解绑;`collaboration.md` 在两触发任一命中时生成、带对应段(STANDARD §3 + §5.2「并行」轴 + sop-init step4 拆生成)。② 方法论——**合并/搬迁护栏后,逐条问"这条原来挂什么闸?合并后还挂那个闸吗?",并拿一个"命中原闸、不命中新闸"的真实项目验**;光 grep"文字在不在"不够。
- **代价 / 边界**:exp-006「减法后逐条核护栏」的**升级**——核的不只是"护栏在不在",还有"挂的闸对不对"。是 exp-010「近似≠覆盖」的姊妹:那条治"只搬大原则漏限定",这条治"搬对内容、挂错触发"。**单点 + 改的是自己的 STANDARD**,故引入了"并行 agent"第三条触发(比 owner 心智的"worktree 与否"多一层)——但它正等于 owner 的 worktree 开关,未破"概念干净"。复验待补:**真跑 taoxi-geo `/sop-init` 增量**验单人多端多 agent 确实拿到协调骨架 + worktree。
- **证据**:exp-012(收档位枚举 · 新眼睛 review 撞出单人多端多 agent 丢协调骨架)

---

### ⚠️ 审计的漂移检查若只朝"增"一个方向看,对 STANDARD 减法后的"删除残留"是盲的
- **dogfood 证伪**:exp-012 是减法(删极简块 + S/C/T 档),静态自审全绿;owner 真复跑 py-script `/sop-audit`(exp-012 唯一未验那条),audit 只把档位词换皮(`S0·C0`→描述、`升S2/C1`→加端/加人),**漏了最该改的**——`直接做` 极简块原封不动没换成标准块。根因不是 skill 没更新(验过是新版),是**「模板版本差」查法(`sop-audit:28`,exp-010 建)单向**:只查"template 有、项目缺的新护栏"(`missing`),不查"template 删了、项目还留着旧块/档"(`stale`)。**减法恰好制造"删除残留",回灌机制只朝增看 → 静默放过。**`grep 词汇清零`查得出本仓没残留,**查不出下游已生成项目里的残留**(要 audit 双向逮)。
- **护栏**:① `sop-audit:28` 模板版本差**加双向比** + kind `stale`(项目留着已删的块/档/文件 = 报 + 建议换现行块);② `STANDARD §5.2 ④删除残留`(authoritative:留着已废极简块 / `S/C/T` 编号 / `collaboration-c2`·`s2-contracts` 等已删名 = 漂移、该换)。**护栏机制要随"增 / 删"两种演进对称**——exp-010 补了"增"方向的"近似≠覆盖",exp-013 补"删"方向的"删除残留"。
- **代价 / 边界**:**exp-002「真跑 > dry-run」的再次应验**——静态(grep/护栏在场/净行数)对"减法残留"天生盲,只有真复跑一个旧产物才现形。**边界**:`stale` 别扩成"凡和模板不一字一样就报"(撞 §5.1 防过度治理 + "合理本地偏离"宽放)——只逮**引用了已删名 / 用了已废块结构**这种硬残留,措辞差异照旧宽放。复验待补:**owner 重跑 py-script audit** 验现在逮得到极简块→标准块。
- **证据**:exp-013(dogfood:exp-012 真复跑 py-script,audit 漏报删除残留)

---

### ⚠️ 想"以真项目为纲"改生成器前,先 verify-by-running 现状——现状可能已是你要造的东西
- 把 `/sop-init` 从"抽象 `templates/` 拼装"改成"母本驱动",方案押"templates/ 是会漂的抽象碎片、运行时现拼易 misfire"。2 轮 clean-context 新眼睛**实跑现状**证伪:`templates/` 早已是**整份具体当前母本**(exp-011/012/013 修复都在)、`/sop-init` 早已 copy-whole+slot(无"现拼碎片"步)、audit 双向 diff 早 ship。大改真值只剩"trigger-layer 重排 + base 骨架 crown freshness",其余全是**重建已存在的东西**。
- **护栏**:改生成器 / 重构前,**先 verify-by-running 现状**(读真实流程 + 真跑一遍),别照 spec 对现状的描述动手——描述常 oversell。砍到诚实残值再动。
- **代价 / 边界**:exp-005「为小缺口立大概念」的同形(这次是改整个生成器方向);owner informed override 仍要重排(长期可维护性 taste)= 正当,只要认清量级是"重排+骨架"非"3 源合成"。
- **证据**:exp-014(2 轮新眼睛 verify-by-running 把 master/ 大改砍成重排+骨架)

### ⚠️ 重排 / 搬迁护栏时,把"往 doc 中段插入"改成"槽替换",守住"不中段动刀"不变量
- master/ 重排把"多端约束块**追加进根 CLAUDE.md §2 中段**"(§2 后面紧跟 §3/§4)——文档中段插入、无锚点。正违反 spec 自己立的"`/sop-init` 只在 slot 处 find/replace、**绝不在 doc 中段动刀**"不变量——而那条不变量**正是 freshness crown 不复埋的命根**。新眼睛逮到,改成 `{{multiend_constraints}}` 槽(单端整行删、多端填)。
- **护栏**:任何"往生成物里插一段"的机制,都做成**显式槽替换**(空值整行删),别做成"找位置插入"。中段插入 = 给"载重位置被挪 / 被埋"开后门。
- **证据**:exp-014(新眼睛 review 逮中段插入违反不变量)

### ⚠️ 生成器类改动,静态全绿也必须真跑一遍生成——模板机制层的洞静态照不出
- exp-014 实现过 **4 镜头独立新眼睛**(承重墙保真 / 挂对闸 / bug / 右尺寸)+ grep **全绿**;dogfood **真跑** `/sop-init` 才逮出:master 文件顶部 `<!-- …占位符… -->` 元注释没被 strip → `{{proj}}` 连头注一起被填成乱码。
- **护栏**:改 `/sop-init`(或任何"读模板生成产物"的工具)后,**必须真跑一次生成 + grep 验产物**;静态查(护栏在场 / grep / review)对"模板机制层"的洞**天生盲**。
- **代价 / 边界**:exp-002 / exp-013「真跑>dry-run」的再次应验,同一条规律第三次咬。**复验待补**:dogfood B(taoxi-geo 增量验三触发)+ C(py-script audit 验 stale)跨项目未跑,A(单端 crown freshness)已过。
- **证据**:exp-014(dogfood 真跑逮 meta-header 没 strip)

---

*(下一条等下一次实验。)*
