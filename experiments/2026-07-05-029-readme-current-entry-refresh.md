# exp-029 · README 当前入口重写

- **日期**:2026-07-05
- **真实任务**:多轮 SOP 迭代后,把 README 更新成当前项目入口
- **本次撒手档位**:L2(无语义新增,按 STANDARD / master / PLAYBOOK 现状重排入口)

---

## 1. 选活 + 定档

README 仍保留早期项目介绍口径,但 STANDARD 已连续长出诚实无知闸、查证闭环、doc/issue/PR 三件套、issue 评论分层等主线。风险不是规则错,而是新读者从 README 看到的是旧叙事。

这次只做入口同步,不改权威规则。

## 2. 便宜验证方案

> 我怎么在 5 分钟内、不逐行读,就看出 AI 做对没?

- 验证手段:
  - README 首屏说明 `STANDARD.md` 是唯一真相源。
  - README 能讲清当前主线:两层模型、一条 flow、结构按现实长、doc/issue/PR 三件套、评论分层。
  - README 不重抄 `STANDARD.md` 细则,只做导航。
  - `git diff --check` 通过。
- 如果上面答不上来 → 先停,说明 README 又变成第二套标准或历史散文。

## 3. 给 AI 的简报

```
目标:
把 README 更新为当前项目入口。

约束:
- 不新增 SOP 语义。
- 不重抄 STANDARD 细则。
- 让第一次看仓的人知道先读什么、当前主线是什么、两个 skill 怎么分工。
- 补 experiment / PLAYBOOK,保持自举闭环。

验收标准:
- README 明确 STANDARD 是唯一真相源。
- README 覆盖当前主线但不过度展开。
- README 链接指向真实文件。
- README 删除早期过时叙事。
```

## 4. AI 跑完,我来评

- **AI 做了哪些决策**:
  - 把 README 从早期设计叙事改成当前入口页。
  - 保留两个 skill、当前主线、设计原则、自举方式、仓库结构、读文件顺序。
  - 不把 STANDARD 细则搬进 README。
- **超出我预期 / 我自己想不到的地方**:
  - 把“给 agent 一条路,不是让它背题”放成 README 的中心句。
- **翻车 / 我得纠偏的地方**:
  - 暂无。

### 评分(10 分制)

| 维度 | 分 | 备注 |
|---|---|---|
| 设计质量(它做得好不好) | /10 | |
| 省力程度(比我自己干省了多少脑子) | /10 | |
| **爽感**(有没有"卧槽这样也行") | /10 | |
| 验证成本(检查它的结果累不累) | /10 | |

## 5. 抽一条教训 → 回填 PLAYBOOK

这次学到:README 只能做当前 STANDARD 的入口投影,不能变成第二套规则;多轮规则迭代后,README 要按主线重写而不是补丁式累加。

→ 已写入 `PLAYBOOK.md`?[x]
